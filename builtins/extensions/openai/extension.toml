id = "openai"
name = "OpenAI Upstream"
description = "提供 llm.generate 能力，并把 OpenAI 兼容上游接入系统 provider 槽位。"
docs = "docs/overview.md"
kind = "extension"

[[links]]
label = "Provider Usage"
target = "docs/provider.md"

[[examples]]
title = "生成回复"
summary = "供 Agent 通过 llm.generate 调用上游模型生成文本。"
input_hint = "llm.generate"

[source]
mode = "package"
root = "."
dev = false

[[capabilities]]
id = "openai.generate"
contract = "llm.generate"
kind = "generate"
entry = "plugins/provider/provider.js"
metadata = { provider = { id = "openai", kind = "openai", extension_id = "openai", interfaces = ["generate", "tools", "models"], model_discovery = true, recommended_model = "gpt-5.4", manual_model = true, generation_options = [
  { id = "reasoning_effort", label = { key = "ext.openai.option.reasoning_effort", fallback = "Reasoning effort" }, value_type = "select", required = false, default_value = "medium", allowed_values = ["low", "medium", "high"] },
] } }
