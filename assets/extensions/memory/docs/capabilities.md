# Memory Capabilities

- `memory.workspace.get`
- `memory.entry.list`
- `memory.episode.list`
- `memory.ingest`
- `memory.query`
- `memory.review`
- `memory.build_context`

说明：

- `memory` 只维护记忆主数据、审查结果、上下文装配和图谱侧车。
- `memory` 不再订阅并镜像保存原始 conversation message 流。
