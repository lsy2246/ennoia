id = "memory"
version = "0.1.0"
name = "Memory"
description = "提供记忆工作台、回忆、审查与上下文组装能力。"
docs = "docs/overview.md"

[compat]
ennoia = ">=0.1.0"

[conversation]
visible = true
resources = ["memory.item"]
operations = [
  "memory.ingest",
  "memory.query",
  "memory.review",
  "memory.build_context",
]

[[views]]
name = "memory.page"
type = "page"
title = { key = "ext.memory.page", fallback = "记忆" }
nav = "sidebar"
order = 30
icon = "memory"
route = "/memory"

[[views]]
name = "memory.about"
type = "page"
title = { key = "ext.memory.about.page", fallback = "关于记忆" }
route = "/memory/about"

[[settings]]
key = "default_owner_kind"
label = { key = "ext.memory.setting.default_owner_kind", fallback = "默认 owner kind" }
description = { key = "ext.memory.setting.default_owner_kind_description", fallback = "决定记忆工作台首次检索时使用的 owner kind。" }
type = "select"
default_value = "global"
options = [
  { label = { key = "ext.memory.setting.option.global", fallback = "全局" }, value = "global" },
  { label = { key = "ext.memory.setting.option.agent", fallback = "Agent" }, value = "agent" },
  { label = { key = "ext.memory.setting.option.space", fallback = "空间" }, value = "space" },
]

[[settings]]
key = "default_recall_limit"
label = { key = "ext.memory.setting.default_recall_limit", fallback = "默认召回数量" }
description = { key = "ext.memory.setting.default_recall_limit_description", fallback = "决定记忆工作台默认回忆时采用的数量上限。" }
type = "number"
default_value = 8

[[operations]]
name = "memory.workspace.get"
description = "读取记忆工作台摘要。"

[[operations]]
name = "memory.entry.list"
description = "列出记忆条目。"
agent = true

[[operations]]
name = "memory.episode.list"
description = "列出记忆 episode。"
agent = true

[[operations]]
name = "memory.ingest"
description = "写入记忆。"
agent = true

[[operations]]
name = "memory.query"
description = "检索记忆。"
agent = true

[[operations]]
name = "memory.review"
description = "审查记忆。"
agent = true

[[operations]]
name = "memory.build_context"
description = "为当前任务组装记忆上下文。"
agent = true
